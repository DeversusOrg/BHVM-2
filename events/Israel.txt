###########################
# Israeli Events
###########################

add_namespace = israel
add_namespace = ISR_leader_events

country_event = {
	id = israel.1 # the bright future of israel
	title = israel.1.t
	desc = israel.1.d
	picture = GFX_report_event_king_speech
	
	is_triggered_only = yes
	immediate = {
		hidden_effect = {
			set_country_flag = ISR_historical_path_chosen	 #Trigger only once
		}
	}
	option = {
		name = israel.1.a # hist option , doesnt allow to do fascist and commie path , doesnt allow the recognition on map focus
	}
	#option = { # non hist option , allows to go fascist and commie , recognition on the map focus is allowed , cant do american and british paths
	#	name = israel.1.b
	#}
	#Wont work for now
}
country_event = {
	id = ISR_leader_events.1
	title = ISR_leader_events.1.t
	desc = ISR_leader_events.1.d
	picture = GFX_report_event_usa_flag

	is_triggered_only = yes

	immediate = {
		# Drop the council the moment the event fires
		retire_country_leader = yes
		remove_ideas = {
			# In case council was an idea token, also clean it up
		}
	}

	# Option A: David Ben Gurion
	option = {
		name = ISR_leader_events.1.a
		ai_chance = { base = 40 }
		set_country_flag = ISR_american_path_chosen
		set_country_flag = ISR_leader_chosen
		activate_mission = ISR_leader_events.3	# Lock out British path
		set_politics = {
			ruling_party = democratic
		}
		add_popularity = {
			ideology = democratic
			popularity = 0.15
		}
		set_country_leader = {
			character = ISR_david_ben_gurion
			ideology = populism
		}
		add_ideas = ISR_american_jewish_support
		custom_effect_tooltip = ISR_council_dissolved_tt
	}

	# Option B: Albert Einstein
	option = {
		name = ISR_leader_events.1.b
		ai_chance = { base = 30 }
		set_country_flag = ISR_american_path_chosen
		set_country_flag = ISR_leader_chosen
		set_politics = {
			ruling_party = democratic
		}
		add_popularity = {
			ideology = democratic
			popularity = 0.10
		}
		set_country_leader = {
			character = ISR_albert_einstein
			ideology = conservatism
		}
		add_ideas = ISR_american_jewish_support
		add_research_slot = 1
		custom_effect_tooltip = ISR_council_dissolved_tt
	}
}

# =============================================
# EVENT 2: British Guarantee Path — Choose Leader
# Fired by focus: ISR_british_guarantees
# =============================================
country_event = {
	id = ISR_leader_events.2
	title = ISR_leader_events.2.t
	desc = ISR_leader_events.2.d
	picture = GFX_report_event_uk_flag

	is_triggered_only = yes

	immediate = {
		retire_country_leader = yes
	}

	# Option A: Benjamin Disraeli
	option = {
		name = ISR_leader_events.2.a
		ai_chance = { base = 50 }
		set_country_flag = ISR_british_path_chosen
		set_country_flag = ISR_leader_chosen
		set_politics = {
			ruling_party = neutrality
		}
		add_popularity = {
			ideology = neutrality
			popularity = 0.15
		}
		set_country_leader = {
			character = ISR_benjamin_disraeli
			ideology = centrism
		}
		add_ideas = ISR_british_mandate_support
		ENG = {
			add_opinion_modifier = {
				target = ISR
				modifier = large_increase
			}
		}
		custom_effect_tooltip = ISR_council_dissolved_tt
	}

	# Option B: Israel Kocach
	option = {
		name = ISR_leader_events.2.b
		ai_chance = { base = 50 }
		set_country_flag = ISR_british_path_chosen
		set_country_flag = ISR_leader_chosen
		set_politics = {
			ruling_party = neutrality
		}
		add_popularity = {
			ideology = neutrality
			popularity = 0.10
		}
		set_country_leader = {
			character = ISR_israel_kocach
			ideology = moderatism
		}
		add_ideas = ISR_british_mandate_support
		add_manpower = 5000
		ENG = {
			add_opinion_modifier = {
				target = ISR
				modifier = large_increase
			}
		}
		custom_effect_tooltip = ISR_council_dissolved_tt
	}
}

# =============================================
# EVENT 3: Mutual Exclusion Notification
# Fires when one path is chosen, locks the other
# =============================================
country_event = {
	id = ISR_leader_events.3
	title = ISR_leader_events.3.t
	desc = ISR_leader_events.3.d
	picture = GFX_report_event_generic_sign

	is_triggered_only = yes

	# Just an acknowledgement event, no choices needed
	option = {
		name = ISR_leader_events.3.a
		# Nothing — the flag already blocks the other focus via available = {}
	}
}

